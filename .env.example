# Online play (optional). Copy this file to ".env" and fill in your
# Supabase project's values, then restart `npm run dev`.
# Without these, the app runs fine in local pass-and-play mode.
#
# Find these in your Supabase project: Settings → API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
